<h1>Add a review</h1>

<%# @review.persisted? => true
@review a une id
--> edit à l'update

@review.persisted? =>
@review n'a pas d'id
de la new au create %>

<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.label :rating %>
  <%= f.text_field :rating %>
  <%= f.submit %>
<% end %>
